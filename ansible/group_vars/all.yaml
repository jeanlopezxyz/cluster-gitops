# Global variables for OpenShift Day-2 Operations
# Red Hat Consulting

# GitOps Configuration (only what Ansible needs for bootstrap)
gitops:
  operator_channel: "latest"
  namespace: "openshift-gitops"
  wait_timeout: 600

# GitOps Repository Configuration
gitops_repository: "https://github.com/jeanlopezxyz/cluster-gitops.git"

# Note: Day-2 application configurations are managed by ArgoCD from Git repository